diff -ru a/prisma/schema.prisma b/prisma/schema.prisma
--- a/prisma/schema.prisma	2026-07-29 05:39:26.623418954 +0000
+++ b/prisma/schema.prisma	2026-07-29 05:39:26.630283882 +0000
@@ -13,16 +13,11 @@
   url      = env("DATABASE_URL")
 }
 
-enum ComicStatus {
-  processing
-  ready
-  error
-}
-
-enum ComicFormat {
-  cbz
-  cbr
-}
+// SQLite has no native enum support in Prisma, so `status` and `format`
+// are plain strings here, constrained to a fixed set of values at the
+// application layer instead (see the Zod schemas / literal checks in
+// src/routes and src/lib). If this ever migrates to Postgres, these two
+// become real Prisma enums with a one-line schema change + migration.
 
 model Comic {
   id        String      @id @default(cuid())
@@ -35,8 +30,8 @@
   summary   String?
 
   fileName    String
-  format      ComicFormat
-  status      ComicStatus @default(processing)
+  format      String // "cbz" | "cbr"
+  status      String @default("processing") // "processing" | "ready" | "error"
   errorMessage String?
 
   pageCount  Int      @default(0)
